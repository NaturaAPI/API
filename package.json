{
  "name": "my-netlify-functions",
  "version": "1.0.0",
  "description": "Netlify Functions using node-fetch",
  "main": "index.js",
  "scripts": {
    "start": "netlify dev",  // 로컬에서 테스트할 때 사용
    "deploy": "netlify deploy"  // 배포할 때 사용
  },
  "dependencies": {
    "node-fetch": "^3.0.0"  // 최신 버전 사용 (ESM 지원)
  },
  "devDependencies": {},
  "engines": {
    "node": "16.x"  // 최신 Node.js 버전
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NaturaAPI/API"
  },
  "keywords": [
    "netlify",
    "serverless",
    "node-fetch"
  ],
  "author": "bulwe",
  "license": "MIT"
}
